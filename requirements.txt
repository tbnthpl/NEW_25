# Pinned to the versions used in dev/UAT to make builds reproducible (M12).
# Use compatible-release operators so security patches still apply on rebuild.
fastapi==0.128.0
uvicorn==0.40.0
python-dotenv==1.2.1
sqlalchemy==2.0.46
oracledb==3.4.1
pandas==3.0.0
openpyxl==3.1.5
python-multipart==0.0.21
pydantic==2.12.5
requests==2.32.5
defusedxml==0.7.1
